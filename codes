import re

pwd = input("Enter password: ")
if len(pwd) >= 8 and re.search(r"[A-Z]", pwd) and re.search(r"\d", pwd) and re.search(r"\W", pwd):
    print("Strong password")
else:
    print("Weak password")
